{
  "name": "research-assistant-license-server",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Phase L1a — Cloudflare Worker that mints, verifies and revokes Research Assistant licences.",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lemonsqueezy/lemonsqueezy.js": "^4.0.0",
    "@noble/ed25519": "^2.1.0",
    "hono": "^4.6.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240821.1",
    "typescript": "~5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
