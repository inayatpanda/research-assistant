import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export default function CompilePage() {
  return (
    <PagePlaceholder
      title="Compile"
      subtitle="Aggregate highlights by section. Generate AI drafts grounded in your annotations."
      phase="Phase 4"
    />
  )
}
