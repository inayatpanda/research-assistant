import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export default function ReaderPage() {
  return (
    <PagePlaceholder
      title="Reader"
      subtitle="Annotate PDFs with the four-colour highlight system."
      phase="Phase 3"
    />
  )
}
